<%= form_with(model: category_classified, local: true) do |form| %>
  <% if category_classified.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(category_classified.errors.count, "error") %> prohibited this category_classified from being saved:</h2>

      <ul>
      <% category_classified.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :category_classified_user_id %>
  </div>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :category_classified_title %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
