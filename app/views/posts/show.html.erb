<p id="notice"><%= notice %></p>

  <div class="section">

    <div class="row">
      <div class="col s12 m12 l12 center">
        <h1 class="header bold"><%= @post.title %></h1>
        <h5 class="grey-text text-darken-2"><%= @post.sub %></h5>
        <div class="divider"></div>

        <blockquote class="left">
          <p class="bold">Por <%= @post.user.name%></p>
          <p>Atualizado em <%= time_ago_in_words(@post.created_at) %></p>
          
        </blockquote>

        <div class="conteudo col s12 m12 l10 offset-l1">
            <%= image_tag @post.image %>
          <p class=""><%= @post.body.html_safe %></p>
        </div>
      </div>
    </div>
</div>       
<!-- LOMADEE - BEGIN -->
<script src="//ad.lomadee.com/banners/script.js?sourceId=35984927&dimension=1&height=90&width=728&method=1&advertisers=5632,6111,5731,5798,5860" type="text/javascript" language="javascript"></script>
<!-- LOMADEE - END -->
<div class="divider"></div>
      <div class="row">
        <h5 class="header">Veja Também</h5>
        <% @vejas.each do |post| %>    
            <div class="col s12 m6 l4">   
              <%= link_to post do %> 
                <div class="card small">
                  <div class="card-image image">
                  <%= image_tag post.image %>
                  </div> 
                  <div class="card-content">
                    <p class="sub2 grey-text text-darken-4 card-sub"><%= post.title %></p>
                    
                  </div>
                  <div class="card-action">
                  <span class="red-text text-darken-2"><%= post.category.title%> </span> - 
                  <span class="red-text text-darken-2"> <%= time_ago_in_words(post.created_at) %></span>
                  </div> 
                </div> 
              <% end %>     
            </div>
        <% end %>
      </div>
  

<%= link_to 'Voltar', posts_path, class: ' volta waves-effect waves-light btn white-text blue' %>
