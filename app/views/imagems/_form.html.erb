<%= form_with(model: imagem, local: true) do |form| %>
  <% if imagem.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(imagem.errors.count, "error") %> prohibited this imagem from being saved:</h2>

      <ul>
      <% imagem.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :avatar %>
    <%= form.file_field :avatar, id: :imagem_avatar %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
